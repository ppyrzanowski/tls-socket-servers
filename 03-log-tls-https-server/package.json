{
  "name": "03-log-tls-https-server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server/index.js",
  "type": "module",
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "pnpm prebuild && pnpm tsc && pnpm postbuild",
    "postbuild": "cp -r ./src/server/certificates ./dist/server/ && cp -r ./src/client/certificates/ ./dist/client/certificates/",
    "server": "cross-env DEBUG=\"express:router\" node dist/server",
    "client": "node dist/client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Patrik Pyranowski",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "cross-env": "^7.0.3",
    "typescript": "^5.3.3"
  }
}
